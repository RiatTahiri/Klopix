import React, { useState } from "react";
import axios from "axios";

function Auth({}) {
  const user = useState("");

  return (
    <div>
      <p>Checking for authentication</p>
    </div>
  );
}

export default Auth;
