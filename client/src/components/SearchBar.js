import React from "react";

const SearchBar = () => {
  return (
    <input
      type="text"
      id="search_bar"
      name="search_bar"
      placeholder="Search"
    ></input>
  );
};

export default SearchBar;
